name: "HomeLab Everywhere"
version: "2607.3.1"
slug: hle
description: "Expose homelab services via HLE tunnels with built-in SSO"
url: "https://github.com/hle-world/ha-addon"

arch:
  - aarch64
  - amd64
  - armv7

startup: application
boot: auto
init: false

ingress: true
ingress_port: 8099
panel_icon: mdi:tunnel
panel_title: HLE

# The API key is managed in the HLE panel, not here — see run.sh. The only
# option is the agent token, because agent mode has no local UI: endpoints are
# declared in the hle.world dashboard instead.
options:
  agent_token: ""
schema:
  agent_token: "password?"

map:
  - data
  - config:rw

hassio_api: true
hassio_role: manager

image: "ghcr.io/hle-world/ha-addon"
