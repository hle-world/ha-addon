name: Auto-release on sync PR merge

# Triggered when a sync/hle-client-v* PR is merged into main.
# Reads the version from hle/config.yaml and creates a GitHub release,
# which in turn triggers the Docker build workflow.

on:
  pull_request:
    types: [closed]
    branches: [main]

jobs:
  release:
    name: Create release
    if: github.event.pull_request.merged == true && startsWith(github.event.pull_request.head.ref, 'sync/hle-client-v')
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Read version from config.yaml
        id: ver
        run: |
          VERSION=$(grep '^version:' hle/config.yaml | sed 's/version: *"\(.*\)"/\1/')
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Create release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION: ${{ steps.ver.outputs.version }}
        run: |
          gh release create "v${VERSION}" \
            --title "v${VERSION}" \
            --notes "Synced to hle-client v${VERSION}. See [hle-client release notes](https://github.com/hle-world/hle-client/releases/tag/v${VERSION}) for details."
