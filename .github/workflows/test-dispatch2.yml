name: Test Dispatch and Repo
on:
  repository_dispatch:
    types: [webapp-release]
  workflow_dispatch:
    inputs:
      version:
        description: "hle-webapp version to sync (e.g. 2604.1.0)"
        required: true
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello"
