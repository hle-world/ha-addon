name: Build & Publish Add-on

on:
  release:
    types: [published]
  push:
    branches: [main]
    paths:
      - 'hle/**'
  pull_request:
    paths:
      - 'hle/**'

# One Docker Hub repo: docker.io/hleworld/ha-addon
# Build jobs push per-arch interim tags (<arch>-<sha>).
# The manifest job combines them into a single multi-arch tag that
# Docker / HA supervisor resolve automatically at pull time.
#
# Runner strategy:
#   aarch64, armv7 → arc-runner-ha-addon (native ARM)
#   amd64          → arc-runner-ha-addon via QEMU binfmt (registered by
#                    containerd on the k8s nodes). If builds fail with
#                    "exec format error", change that entry to ubuntu-latest.

env:
  IMAGE: docker.io/${{ secrets.DOCKERHUB_USERNAME }}/ha-addon

jobs:
  build:
    name: Build ${{ matrix.arch }}
    runs-on: arc-runner-ha-addon
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: aarch64
            platform: linux/arm64
            base: ghcr.io/home-assistant/aarch64-base-python:3.12
          - arch: amd64
            platform: linux/amd64
            base: ghcr.io/home-assistant/amd64-base-python:3.12
          - arch: armv7
            platform: linux/arm/v7
            base: ghcr.io/home-assistant/armv7-base-python:3.12

    steps:
      - uses: actions/checkout@v4

      - name: Install buildah (if not present)
        run: |
          if ! command -v buildah &>/dev/null; then
            sudo apt-get update -qq && sudo apt-get install -y -qq buildah
          fi

      - name: Login to Docker Hub
        if: github.event_name != 'pull_request'
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
        run: buildah login --username "$DOCKERHUB_USERNAME" --password "$DOCKERHUB_TOKEN" docker.io

      - name: Build
        run: |
          buildah build \
            --platform "${{ matrix.platform }}" \
            --build-arg BUILD_FROM="${{ matrix.base }}" \
            --tag "${{ env.IMAGE }}:${{ matrix.arch }}-${{ github.sha }}" \
            ./hle

      - name: Push interim arch tag
        if: github.event_name != 'pull_request'
        run: buildah push "${{ env.IMAGE }}:${{ matrix.arch }}-${{ github.sha }}"

  manifest:
    name: Create multi-arch manifest
    runs-on: arc-runner-ha-addon
    needs: build
    if: github.event_name == 'release'

    steps:
      - name: Install buildah (if not present)
        run: |
          if ! command -v buildah &>/dev/null; then
            sudo apt-get update -qq && sudo apt-get install -y -qq buildah
          fi

      - name: Login to Docker Hub
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
        run: buildah login --username "$DOCKERHUB_USERNAME" --password "$DOCKERHUB_TOKEN" docker.io

      - name: Create and push manifest
        run: |
          VERSION="${{ github.event.release.tag_name }}"

          for TAG in "$VERSION" latest; do
            buildah manifest create "${{ env.IMAGE }}:${TAG}"
            for ARCH in aarch64 amd64 armv7; do
              buildah manifest add "${{ env.IMAGE }}:${TAG}" \
                "${{ env.IMAGE }}:${ARCH}-${{ github.sha }}"
            done
            buildah manifest push --all "${{ env.IMAGE }}:${TAG}" \
              "docker://${{ env.IMAGE }}:${TAG}"
          done

      - name: Summary
        run: |
          VERSION="${{ github.event.release.tag_name }}"
          echo "### Published \`hleworld/ha-addon:${VERSION}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "Multi-arch manifest covering: aarch64, amd64, armv7" >> "$GITHUB_STEP_SUMMARY"
