name: Sync Frontend from hle-webapp

# Triggered by hle-webapp after it builds a new release (repository_dispatch)
# or manually via workflow_dispatch for testing.
# Downloads the pre-built ha-addon frontend artifact and opens a PR
# updating hle/static/ -- the Docker build just COPYs this directory.

on:
  repository_dispatch:
    types: [webapp-release]
  workflow_dispatch:
    inputs:
      version:
        description: "hle-webapp version to sync (e.g. 2604.1.0)"
        required: true
      hle_client_version:
        description: "hle-client version to pin in Dockerfile (optional)"
        required: false

permissions:
  contents: write
  pull-requests: write

jobs:
  sync:
    runs-on: ubuntu-latest
    env:
      VERSION: ${{ github.event.client_payload.version || github.event.inputs.version }}
      CLIENT_VERSION: ${{ github.event.client_payload.hle_client_version || github.event.inputs.hle_client_version }}
    steps:
      - uses: actions/checkout@v6

      - name: Download ha-addon frontend artifact
        env:
          GH_TOKEN: ${{ secrets.HLE_PAT }}
        run: |
          mkdir -p hle/static
          rm -rf hle/static/*
          curl -sL \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            "https://github.com/hle-world/hle-webapp/releases/download/v${VERSION}/hle-frontend-ha-addon-${VERSION}.tar.gz" \
            | tar -xz -C hle/static

      - name: Sync canonical backend modules from hle-webapp
        env:
          GH_TOKEN: ${{ secrets.HLE_PAT }}
        run: |
          # hle-webapp/backend/{models,tunnel_manager,hle_api}.py are the
          # source of truth for tunnel lifecycle / relay-API plumbing.
          # main.py stays addon-specific (Supervisor API, HA paths).
          tmp=$(mktemp -d)
          for f in models.py tunnel_manager.py hle_api.py; do
            curl -fsSL \
              -H "Authorization: Bearer ${GH_TOKEN}" \
              -H "Accept: application/vnd.github.v3.raw" \
              "https://api.github.com/repos/hle-world/hle-webapp/contents/backend/${f}?ref=v${VERSION}" \
              -o "$tmp/$f"
            if [ ! -s "$tmp/$f" ]; then
              echo "Failed to fetch $f from hle-webapp v${VERSION}" >&2
              exit 1
            fi
            mv "$tmp/$f" "hle/backend/$f"
          done
          # hle-webapp and ha-addon don't share the same ruff format config,
          # so re-format the synced files to match this repo's lint rules.
          pip install --quiet ruff
          ruff format hle/backend/models.py hle/backend/tunnel_manager.py hle/backend/hle_api.py

      - name: Bump hle-client pin in Dockerfile
        if: env.CLIENT_VERSION != ''
        run: |
          CURRENT=$(grep -oE 'hle-client==[0-9][0-9.]*' hle/Dockerfile | head -1 | sed 's/hle-client==//')
          if [ "$CURRENT" != "$CLIENT_VERSION" ]; then
            sed -i "s|hle-client==[0-9][0-9.]*|hle-client==${CLIENT_VERSION}|g" hle/Dockerfile
            echo "Bumped hle-client pin: $CURRENT -> $CLIENT_VERSION"
          else
            echo "hle-client pin already at $CLIENT_VERSION — no change."
          fi

      - name: Open sync PR
        env:
          GH_TOKEN: ${{ secrets.HLE_PAT }}
        run: |
          if git diff --quiet && [ -z "$(git ls-files --others --exclude-standard hle/static/ hle/backend/)" ]; then
            echo "No changes -- skipping PR."
            exit 0
          fi

          BRANCH="chore/sync-webapp-${VERSION}"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -b "${BRANCH}"
          git add hle/static/ hle/backend/models.py hle/backend/tunnel_manager.py hle/backend/hle_api.py hle/Dockerfile
          if [ -n "$CLIENT_VERSION" ]; then
            COMMIT_MSG="chore: sync from hle-webapp v${VERSION} (hle-client v${CLIENT_VERSION})"
            BODY="Pre-built ha-addon frontend from hle-webapp v${VERSION}. hle-client pin bumped to v${CLIENT_VERSION}."
          else
            COMMIT_MSG="chore: sync frontend from hle-webapp v${VERSION}"
            BODY="Pre-built ha-addon frontend from hle-webapp v${VERSION}."
          fi
          git commit -m "${COMMIT_MSG}"
          git push -u origin "${BRANCH}"
          gh pr create \
            --title "${COMMIT_MSG}" \
            --body "${BODY}" \
            --base main
