name: Build & Publish Add-on

on:
  release:
    types: [published]
  push:
    branches: [main]
    paths:
      - 'hle/**'
  pull_request:
    paths:
      - 'hle/**'

jobs:
  build:
    name: Build ${{ matrix.arch }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - arch: aarch64
            base: ghcr.io/home-assistant/aarch64-base-python:3.12
            platform: linux/arm64
          - arch: amd64
            base: ghcr.io/home-assistant/amd64-base-python:3.12
            platform: linux/amd64
          - arch: armhf
            base: ghcr.io/home-assistant/armhf-base-python:3.12
            platform: linux/arm/v6
          - arch: armv7
            base: ghcr.io/home-assistant/armv7-base-python:3.12
            platform: linux/arm/v7
          - arch: i386
            base: ghcr.io/home-assistant/i386-base-python:3.12
            platform: linux/386

    steps:
      - uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        if: github.event_name == 'release'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build (and push on release)
        uses: docker/build-push-action@v5
        with:
          context: hle
          platforms: ${{ matrix.platform }}
          build-args: BUILD_FROM=${{ matrix.base }}
          push: ${{ github.event_name == 'release' }}
          tags: ghcr.io/hle-world/ha-addon/${{ matrix.arch }}:${{ github.event.release.tag_name || 'dev' }}
