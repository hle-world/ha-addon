name: Build & Publish Add-on

on:
  release:
    types: [prereleased]
  workflow_dispatch:

env:
  IMAGE: ghcr.io/hle-world/ha-addon

jobs:
  # Frontend is pre-built by hle-webapp and committed to hle/static/ via sync-webapp.yml.
  # No Node.js build step needed — the Docker image just COPYs hle/static/.

  # ARM builds run on ARC (native ARM). ARC Kubernetes container mode requires
  # a container: field, so we use buildah with --storage-driver vfs --isolation chroot.
  build-arm:
    name: Build ${{ matrix.arch }}
    runs-on: arc-runner-ha-addon
    container:
      image: debian:bookworm-slim
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: aarch64
            platform: linux/arm64
            base: ghcr.io/home-assistant/aarch64-base-python:3.12-alpine3.18
          - arch: armv7
            platform: linux/arm/v7
            base: ghcr.io/home-assistant/armv7-base-python:3.12-alpine3.18

    steps:
      - name: Install tools
        run: apt-get update && apt-get install -y git buildah ca-certificates

      - uses: actions/checkout@v6

      - name: Login to ghcr.io
        if: github.event_name != 'pull_request'
        run: |
          buildah --storage-driver vfs login \
            --username "${{ github.actor }}" \
            --password "${{ secrets.GITHUB_TOKEN }}" \
            ghcr.io

      - name: Build
        run: |
          buildah --storage-driver vfs build \
            --isolation chroot \
            --platform "${{ matrix.platform }}" \
            --build-arg BUILD_FROM="${{ matrix.base }}" \
            --tag "${{ env.IMAGE }}:${{ matrix.arch }}-${{ github.sha }}" \
            ./hle

      - name: Push interim arch tag
        if: github.event_name != 'pull_request'
        run: |
          buildah --storage-driver vfs push \
            "${{ env.IMAGE }}:${{ matrix.arch }}-${{ github.sha }}"

  # amd64 runs on GitHub-hosted ubuntu-latest (native x86_64, no QEMU needed).
  # No container: field — Docker is pre-installed on the runner VM.
  build-amd64:
    name: Build amd64
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v6

      - name: Login to ghcr.io
        if: github.event_name != 'pull_request'
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: Build
        run: |
          docker build \
            --build-arg BUILD_FROM="ghcr.io/home-assistant/amd64-base-python:3.12-alpine3.18" \
            --tag "${{ env.IMAGE }}:amd64-${{ github.sha }}" \
            ./hle

      - name: Push interim arch tag
        if: github.event_name != 'pull_request'
        run: docker push "${{ env.IMAGE }}:amd64-${{ github.sha }}"

  manifest:
    name: Create multi-arch manifest
    # Run on ubuntu-latest (no container) — ARC k8s container mode has a bug
    # with its step runner that causes buildah login to fail intermittently.
    # Docker buildx imagetools is pre-installed on ubuntu-latest and works fine.
    runs-on: ubuntu-latest
    needs: [build-arm, build-amd64]
    if: github.event_name == 'release'
    permissions:
      contents: read
      packages: write

    steps:
      - name: Login to ghcr.io
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: Create and push manifest
        env:
          VERSION: ${{ github.event.release.tag_name }}
        run: |
          VERSION="${VERSION#v}"  # strip leading 'v' so tag matches config.yaml version
          docker buildx imagetools create \
            --tag "${{ env.IMAGE }}:${VERSION}" \
            --tag "${{ env.IMAGE }}:latest" \
            "${{ env.IMAGE }}:aarch64-${{ github.sha }}" \
            "${{ env.IMAGE }}:amd64-${{ github.sha }}" \
            "${{ env.IMAGE }}:armv7-${{ github.sha }}"

      - name: Summary
        env:
          VERSION: ${{ github.event.release.tag_name }}
        run: |
          VERSION="${VERSION#v}"
          echo "### Published \`ghcr.io/hle-world/ha-addon:${VERSION}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "Multi-arch manifest covering: aarch64, amd64, armv7" >> "$GITHUB_STEP_SUMMARY"

  # Bump config.yaml AFTER the image is confirmed pushed. This ensures config.yaml
  # version matches the available Docker image before HA Supervisor can see it.
  # The release intentionally stays as a pre-release so you can test on HA with
  # "Show beta versions" enabled. Promote to full release manually in GitHub UI
  # when testing is complete — HA Supervisor will then offer it to all users.
  publish:
    name: Bump config version
    runs-on: ubuntu-latest
    needs: manifest
    if: github.event_name == 'release'
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v6
        with:
          # Use PAT so the version bump commit triggers downstream workflows
          # (GITHUB_TOKEN commits are silently ignored by Actions).
          token: ${{ secrets.HLE_PAT }}
          ref: main

      - name: Bump config.yaml version
        env:
          VERSION: ${{ github.event.release.tag_name }}
        run: |
          VERSION="${VERSION#v}"
          sed -i "s/^version: .*/version: \"${VERSION}\"/" hle/config.yaml
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add hle/config.yaml
          # No-op if version was already set (e.g. re-running the workflow).
          git diff --staged --quiet || git commit -m "Bump version to ${VERSION} [skip ci]"
          git push
