name: Sync Frontend from hle-webapp

# Triggered by hle-webapp after it builds a new release.
# Downloads the pre-built ha-addon frontend artifact and opens a PR
# updating hle/static/ — the Docker build just COPYs this directory.

on:
  repository_dispatch:
    types: [webapp-release]
  workflow_dispatch:
    inputs:
      version:
        description: "hle-webapp version to sync (e.g. 2604.1.0)"
        required: true

permissions:
  contents: write
  pull-requests: write

jobs:
  sync:
    runs-on: ubuntu-latest
    env:
      VERSION: ${{ github.event.client_payload.version || github.event.inputs.version }}
    steps:
      - uses: actions/checkout@v6

      - name: Download ha-addon frontend artifact
        env:
          GH_TOKEN: ${{ secrets.HLE_PAT }}
        run: |
          mkdir -p hle/static
          rm -rf hle/static/*
          curl -sL \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            "https://github.com/hle-world/hle-webapp/releases/download/v${VERSION}/hle-frontend-ha-addon-${VERSION}.tar.gz" \
            | tar -xz -C hle/static

      - name: Open sync PR
        env:
          GH_TOKEN: ${{ secrets.HLE_PAT }}
        run: |
          if git diff --quiet && [ -z "$(git ls-files --others --exclude-standard hle/static/)" ]; then
            echo "No frontend changes — skipping PR."
            exit 0
          fi

          BRANCH="chore/sync-webapp-${VERSION}"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -b "${BRANCH}"
          git add hle/static/
          git commit -m "chore: sync frontend from hle-webapp v${VERSION}"
          git push -u origin "${BRANCH}"
          gh pr create \
            --title "chore: sync frontend from hle-webapp v${VERSION}" \
            --body "Pre-built ha-addon frontend from hle-webapp v${VERSION}.

No frontend source lives in this repo — all UI changes go through hle-webapp." \
            --base main
