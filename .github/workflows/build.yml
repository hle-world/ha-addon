name: Build & Publish Add-on

on:
  release:
    types: [published]
  push:
    branches: [main]
    paths:
      - 'hle/**'
  pull_request:
    paths:
      - 'hle/**'

# Per-arch images: docker.io/<DOCKERHUB_USERNAME>/ha-addon-<arch>
# HA supervisor substitutes {arch} from config.yaml `image` field.
#
# Runner strategy:
#   aarch64, armv7 → arc-runner-ha-addon (native ARM, no emulation needed)
#   amd64          → arc-runner-ha-addon via QEMU binfmt (k8s nodes register
#                    handlers automatically via containerd).
#                    If amd64 builds fail, change its runner to ubuntu-latest.

jobs:
  build:
    name: Build ${{ matrix.arch }}
    runs-on: arc-runner-ha-addon
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: aarch64
            platform: linux/arm64
            base: ghcr.io/home-assistant/aarch64-base-python:3.12
          - arch: amd64
            platform: linux/amd64
            base: ghcr.io/home-assistant/amd64-base-python:3.12
          - arch: armv7
            platform: linux/arm/v7
            base: ghcr.io/home-assistant/armv7-base-python:3.12

    steps:
      - uses: actions/checkout@v4

      - name: Install buildah (if not present)
        run: |
          if ! command -v buildah &>/dev/null; then
            sudo apt-get update -qq && sudo apt-get install -y -qq buildah
          fi
          buildah version

      - name: Login to Docker Hub
        if: github.event_name != 'pull_request'
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
        run: |
          buildah login \
            --username "$DOCKERHUB_USERNAME" \
            --password "$DOCKERHUB_TOKEN" \
            docker.io

      - name: Build image
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
        run: |
          IMAGE="docker.io/${DOCKERHUB_USERNAME}/ha-addon-${{ matrix.arch }}"
          VERSION="${{ github.event.release.tag_name || github.sha }}"

          buildah build \
            --platform "${{ matrix.platform }}" \
            --build-arg BUILD_FROM="${{ matrix.base }}" \
            --tag "${IMAGE}:${VERSION}" \
            ./hle

          # Smoke-check: image should exist locally
          buildah images "${IMAGE}:${VERSION}"

      - name: Push to Docker Hub
        if: github.event_name == 'release'
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
        run: |
          IMAGE="docker.io/${DOCKERHUB_USERNAME}/ha-addon-${{ matrix.arch }}"
          VERSION="${{ github.event.release.tag_name }}"

          buildah push "${IMAGE}:${VERSION}"

          # Retag and push :latest
          buildah tag "${IMAGE}:${VERSION}" "${IMAGE}:latest"
          buildah push "${IMAGE}:latest"

  # After all arch images are pushed, update the Docker Hub description
  # (optional — requires DOCKERHUB_TOKEN with write:description scope)
  post:
    name: Post-release
    runs-on: arc-runner-ha-addon
    needs: build
    if: github.event_name == 'release'
    steps:
      - name: Summary
        run: |
          echo "### Released ${{ github.event.release.tag_name }}" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          for arch in aarch64 amd64 armv7; do
            echo "- \`docker.io/${{ secrets.DOCKERHUB_USERNAME }}/ha-addon-${arch}:${{ github.event.release.tag_name }}\`" >> "$GITHUB_STEP_SUMMARY"
          done
