name: Auto-release on PR merge

# Creates a GitHub PRE-RELEASE when any PR is merged to main.
# This triggers the Docker build workflow (build.yml), which builds
# multi-arch images, pushes to GHCR, bumps config.yaml, and promotes
# the pre-release to full — preventing HA Supervisor from seeing
# the new version before the image is available.
#
# Version logic:
#   - sync/hle-client-v* PRs: version from Dockerfile hle-client== pin
#   - All other PRs: bump patch digit from current config.yaml version
#
# Skip release: add [skip release] to the PR title

on:
  pull_request:
    types: [closed]
    branches: [main]

jobs:
  release:
    name: Create pre-release
    if: >-
      github.event.pull_request.merged == true &&
      !contains(github.event.pull_request.title, '[skip release]')
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v6

      - name: Determine version
        id: ver
        env:
          HEAD_REF: ${{ github.event.pull_request.head.ref }}
        run: |
          if [[ "$HEAD_REF" == sync/hle-client-v* ]]; then
            # Client sync: version from Dockerfile pin + .0 patch suffix
            CLIENT_VER=$(grep 'hle-client==' hle/Dockerfile | sed 's/.*hle-client==\([0-9][0-9.]*\).*/\1/')
            VERSION="${CLIENT_VER}.0"
            echo "source=client-sync" >> "$GITHUB_OUTPUT"
          else
            # Addon-only: bump patch from config.yaml
            CURRENT=$(grep '^version:' hle/config.yaml | sed 's/.*"\(.*\)"/\1/')
            MAJOR=$(echo "$CURRENT" | cut -d. -f1)
            MINOR=$(echo "$CURRENT" | cut -d. -f2)
            PATCH=$(echo "$CURRENT" | cut -d. -f3)
            VERSION="${MAJOR}.${MINOR}.$((PATCH + 1))"
            echo "source=addon-only" >> "$GITHUB_OUTPUT"
          fi
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Create pre-release
        env:
          # Must use a PAT — GITHUB_TOKEN-created releases do not cascade
          # to trigger other workflows (GitHub anti-loop protection).
          GH_TOKEN: ${{ secrets.HLE_PAT }}
          VERSION: ${{ steps.ver.outputs.version }}
          SOURCE: ${{ steps.ver.outputs.source }}
          PR_TITLE: ${{ github.event.pull_request.title }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          if [[ "$SOURCE" == "client-sync" ]]; then
            NOTES="Synced to hle-client v${VERSION}. See [hle-client release notes](https://github.com/hle-world/hle-client/releases/tag/v${VERSION}) for details."
          else
            NOTES="Addon update: ${PR_TITLE} (#${PR_NUMBER})"
          fi
          gh release create "v${VERSION}" \
            --prerelease \
            --title "v${VERSION}" \
            --notes "$NOTES"

      - name: Notify hle-docker of addon changes
        if: steps.ver.outputs.source == 'addon-only'
        env:
          GH_TOKEN: ${{ secrets.HLE_PAT }}
          VERSION: ${{ steps.ver.outputs.version }}
          PR_TITLE: ${{ github.event.pull_request.title }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          PR_URL: ${{ github.event.pull_request.html_url }}
        run: |
          gh api repos/hle-world/hle-docker/dispatches \
            -f event_type=ha-addon-changes \
            -f "client_payload[version]=${VERSION}" \
            -f "client_payload[pr_title]=${PR_TITLE}" \
            -f "client_payload[pr_number]=${PR_NUMBER}" \
            -f "client_payload[pr_url]=${PR_URL}"

      # The hle-webapp dispatch was removed alongside hle-webapp PR #42
      # (rogue sync-hle-docker.yml) and hle-docker PR #71. hle-webapp is
      # the source of truth for the frontend; ha-addon consumes via
      # sync-frontend.yml, never the reverse.
